import { TextTruncarPipe } from './text-truncar.pipe';

describe('TextTruncarPipe', () => {
  it('create an instance', () => {
    const pipe = new TextTruncarPipe();
    expect(pipe).toBeTruthy();
  });
});
